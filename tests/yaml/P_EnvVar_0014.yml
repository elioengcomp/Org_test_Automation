pipelines:
  - name: P_EnvVar_0014
    configuration:
      environmentVariables:
        readOnly:
          pipe_env_var:
            default: value1
            values:
              - value1
              - value2
            allowCustom: true
    steps:
      - name: P_EnvVar_0014_1
        type: Bash
        execution:
          onExecute:
            - echo $pipe_env_var

