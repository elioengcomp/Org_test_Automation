pipelines:
  - name: P_EnvVar_0020
    configuration:
      environmentVariables:
        readOnly:
          pipe_env_var:
            default: value1
            values: value1,value2
    steps:
      - name: P_EnvVar_0020_1
        type: Bash
        execution:
          onExecute:
            - echo $pipe_env_var
