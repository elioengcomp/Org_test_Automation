pipelines:
  - name: P_EnvVar_0013
    configuration:
      environmentVariables:
        readOnly:
          pipe_env_var:
            default: default_value
    steps:
      - name: P_EnvVar_0013_1
        type: Bash
        execution:
          onExecute:
            - echo $pipe_env_var

